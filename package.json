{
  "name": "tvm.ilya.ya",
  "version": "1.0.7",
  "description": "tvm",
  "author": "iyasiuk@elinext.com",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ilyaElinext/TVM.git"
  },
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "build": {
    "win": {
      "target": "nsis",
      "icon": "icon.png",
      "legalTrademarks": "flowbird"
    }
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-builder": "^20.40.2"
  },
  "dependencies": {
    "electron-updater": "^4.0.6"
  }
}
